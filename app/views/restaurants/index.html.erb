<h1>Les restaurants</h1>
<% @restaurants.each do |restaurant| %>
  <%= link_to restaurant.name, restaurant_path(restaurant) %>
  <button type="button" class="btn btn-light"><%= link_to 'Add a review', restaurant_reviews_path(restaurant) %></button>
    <p><%= restaurant.address %></p>
  <br>
<% end %>
<br>
<button type="button" class="btn btn-light"><%= link_to 'Ajouter un nouveau restaurant', new_restaurant_path %></button>
<br>
<br>
